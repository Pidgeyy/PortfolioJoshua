Portfolio scaffold

Files created:
- index.html — main HTML layout
- styles.css — layout and styling
- script.js — small JS for mobile nav toggle

Open `index.html` in your browser to view the layout. Adjust content and images in `index.html` and `assets/` as needed.